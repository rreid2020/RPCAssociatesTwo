# apps

Transitional root for deployable applications.

Current source-of-truth apps remain:
- Root SPA (`src/`)
- `api/server`
- `client-portal/taxgpt-web`
- `client-portal/taxgpt-api`

This folder is intentionally scaffolded for future incremental consolidation under the strangler plan.

