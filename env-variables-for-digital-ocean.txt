# Copy and paste these into Digital Ocean Component-Level Environment Variables
# When adding via .env file upload, Digital Ocean will detect the scope automatically
# You may need to manually set BUILD_TIME vs RUN_TIME scope after uploading

# ============================================
# RUNTIME VARIABLES (RUN_TIME scope)
# ============================================
NODE_ENV=production
PORT=3000

# Database Configuration (PostgreSQL - Digital Ocean Managed Database)
DB_HOST=db-postgresql-tor1-60715-do-user-20752759-0.e.db.ondigitalocean.com
DB_PORT=25060
DB_NAME=defaultdb
DB_USER=doadmin
DB_PASSWORD=your-database-password-here
DB_SSL=true

# Email Configuration (Resend)
RESEND_API_KEY=your-resend-api-key-here
EMAIL_FROM="RPC Associates <contacts@rpcassociates.co>"
NOTIFICATION_EMAIL=contacts@rpcassociates.co

# CORS Configuration
ALLOWED_ORIGINS=https://axiomft.ca,https://www.axiomft.ca,http://localhost:5173

# ============================================
# BUILD-TIME VARIABLES (BUILD_TIME scope)
# ============================================
# Note: These might already be set at app-level. 
# If they are, you can skip them here or they'll override app-level values
# Match sanity-studio/sanity.config.ts (default project) unless you use a different project in production:
VITE_SANITY_PROJECT_ID=t3zpylpm
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01
VITE_SANITY_USE_CDN=true
# If articles are empty in production, ensure these are BUILD_TIME (or RUN_AND_BUILD_TIME) on the web component
# and add CORS origins in https://www.sanity.io/manage → your project → API → CORS (https://axiomft.ca, https://www.axiomft.ca)
VITE_API_BASE_URL=https://axiomft.ca

# Clerk Authentication (BUILD_TIME scope - required for client portal)
# Get your key from https://dashboard.clerk.com → API Keys → RPCTaxGPT app
# On Free Plan, use pk_test_... (works for both dev and production)
# Production keys (pk_live_...) only available on paid plans
VITE_CLERK_PUBLISHABLE_KEY=pk_test_cm9tYW50aWMtc3RhbGxpb24tODIuY2xlcmsuYWNjb3VudHMuZGV2JA
