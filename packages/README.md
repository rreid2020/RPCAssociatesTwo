# packages

Transitional root for shared reusable packages.

Current source-of-truth shared packages remain under:
- `client-portal/packages/*`

This root folder is scaffolded for gradual extraction of cross-app reusable contracts and utilities.

