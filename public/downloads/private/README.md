# Private downloads (unlisted)

Files here are publicly reachable if someone knows the exact URL, but they are not linked from navigation or the sitemap, and are disallowed in `robots.txt`.

Use only for share-by-link assets (e.g. CV). Do not put secrets or highly sensitive data here — use the client portal for that.
