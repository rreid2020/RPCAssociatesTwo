# src/app

Application bootstrap layer for root composition concerns.

Planned contents:
- App bootstrap wiring
- top-level providers composition
- runtime guards used by route registries

Current source-of-truth remains `src/main.tsx` and `src/App.tsx` until routing extraction completes.

