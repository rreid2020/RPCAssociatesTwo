# src/hooks

Shared cross-module React hooks.

Current hooks are distributed across components/features and `src/lib/subscriptions/hooks.ts`.
New shared hooks should be added here with explicit domain ownership notes.

