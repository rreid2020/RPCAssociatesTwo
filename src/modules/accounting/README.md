# accounting module

Domain boundary for accounting workspace and accounting administration.

Current compatibility source:
- UI implementation remains in `src/pages/portal/accounting/AccountingWorkspacePage.tsx`
- Route registry is bridged via `src/modules/accounting/routes/index.ts`

