# auth module

Domain boundary for portal sign-in/sign-up and SSO callback route flows.

Current compatibility source:
- Existing auth pages remain in `src/pages/portal/SignIn.tsx` and `src/pages/portal/SignUp.tsx`
- Route registry is bridged via `src/modules/auth/routes/index.ts`

