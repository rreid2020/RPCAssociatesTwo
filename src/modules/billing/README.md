# billing module

Domain boundary for workspace-level billing, subscription management, usage, and upgrade flows.

Compatibility source:
- Service contracts live in `src/services/billing/*`.
- UI routes/pages in this module consume those contracts without embedding provider-specific logic.
