# documents module

Domain boundary for file repository, supporting document linkage, and lineage-aware document operations.

Current compatibility source:
- Existing document UX remains in `src/pages/portal/FileRepository.tsx` and accounting engagement document views.

