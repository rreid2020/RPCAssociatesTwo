# subscriptions module

Domain boundary for feature access and subscription-plan gates.

Current compatibility source:
- Existing plan and access logic remains in `src/lib/subscriptions/*` and portal shell gating.

