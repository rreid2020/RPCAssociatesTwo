# src/providers

Application-wide provider composition boundary.

Planned contents:
- auth-aware provider composition
- feature flag provider wrappers
- analytics/logging context providers

Current provider composition remains in `src/main.tsx`.

