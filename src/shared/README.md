# src/shared

Cross-domain reusable UI and platform primitives.

Planned subfolders:
- `ui/`, `layouts/`, `tables/`, `forms/`, `modals/`, `charts/`
- `navigation/`, `loading/`, `errors/`, `typography/`

Current reusable components remain in `src/components` and will be extracted incrementally.

