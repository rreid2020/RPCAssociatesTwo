# src/styles Migration Note

Styling remains backward compatible and unchanged by architecture migration.

This folder continues to own shared styling entrypoints.
Module-level style files may be introduced only where they do not alter existing visual branding or UX patterns.

